general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid stacking lock instances
    before_sleep_cmd = loginctl lock-session    # lock before suspend / lid close
    after_sleep_cmd = hyprctl dispatch dpms on  # wake the screen on resume
}

listener {
    timeout = 300                               # 5 min — screen off
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 600                               # 10 min — lock
    on-timeout = loginctl lock-session
}
